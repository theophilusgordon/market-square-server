version: '3.3'
services:
  server:
    container_name: market-square-server
    restart: always
    build: .
    ports:
      - "8080:8080"
    links:
      - market-square-db
  db:
    container_name: market-square-db
    image: postgres
    ports:
        - '5432:6432'
